"use strict";
{
    // Basic data type
    // string
    let firstName = "Rayhan"; // Type Inference defined
    let lastName = "Mojumdar"; // Type explicit defined
    // number
    let roll = 123;
    // boolean
    let isAdmin = true;
    // undefined
    let x = undefined;
    // null
    let y = null;
    // any types
    let d;
    d = 123;
    d = "text";
    d = true;
    let c;
    c = 69;
    // Array
    // only string add in this array
    let friends = ["rachel", "monica"];
    // only number add in this array
    let eligibleRollList = [1, 2, 3, 4];
    // tuple --> array --> order --> type of values
    let coordinates = [1, 5];
    let ageName = [32, "Rayhan", true];
    // ageName[0] = 'Mojumdar' there was an tuple type error
}
